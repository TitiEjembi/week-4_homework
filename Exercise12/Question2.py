#what is going on? the difference between the two:
#can you explain the output? Return the length (the number of characters in the string hello) while the second returns the no as a single element/item).
tup = 'Hello'
print(len(tup))
#print 5

tup = 'Hello',
print(len(tup))
#prints 1
